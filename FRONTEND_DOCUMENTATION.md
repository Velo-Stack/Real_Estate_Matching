# 📱 توثيق الـ Frontend - نظام المطابقة العقاري

> شرح كامل لبنية الـ Frontend الحالية والـ Components والـ Pages والـ Utilities

---

## 🏗️ البنية العامة للمشروع

```
frontend/
├── src/
│   ├── main.jsx                 # نقطة الدخول الرئيسية
│   ├── App.jsx                  # تطبيق رئيسي (التوجيه)
│   ├── index.css                # أنماط عامة
│   ├── App.css                  # أنماط التطبيق
│   │
│   ├── components/              # المكونات القابلة لإعادة الاستخدام
│   │   ├── Layout.jsx           # تخطيط الصفحة (Sidebar + Main)
│   │   ├── Sidebar.jsx          # القائمة الجانبية
│   │   ├── Modal.jsx            # نافذة منبثقة
│   │   └── Table.jsx            # جدول ذكي
│   │
│   ├── pages/                   # صفحات التطبيق
│   │   ├── Login.jsx            # صفحة تسجيل الدخول
│   │   ├── Dashboard.jsx        # لوحة التحكم (داخل App.jsx حالياً)
│   │   ├── Offers.jsx           # صفحة العروض
│   │   ├── Requests.jsx         # صفحة الطلبات
│   │   └── Matches.jsx          # صفحة المطابقات
│   │
│   ├── context/                 # Context API
│   │   └── AuthContext.jsx      # إدارة المصادقة
│   │
│   ├── utils/                   # دوال مساعدة
│   │   └── api.js               # إعدادات axios والاتصال بالـ Backend
│   │
│   ├── styles/                  # ملفات CSS
│   │   ├── Layout.css
│   │   ├── Login.css
│   │   ├── Modal.css
│   │   └── Table.css
│   │
│   └── assets/                  # صور وملفات ثابتة
│
├── package.json
├── vite.config.js
├── eslint.config.js
├── index.html
└── README.md
```

---

## 📚 الدليل المفصل

### 1️⃣ الملفات الأساسية

#### `main.jsx` - نقطة الدخول
```jsx
import App from './App.jsx'
createRoot(document.getElementById('root')).render(...)
```
- يحمل `App` component في `<div id="root">` في `index.html`
- يستخدم `StrictMode` للتحقق من الأخطاء أثناء التطوير

---

#### `App.jsx` - التطبيق الرئيسي
**المهام:**
- إعداد `React Router` (التوجيه)
- تغليف التطبيق بـ `AuthProvider` لإدارة المصادقة
- إنشاء `ProtectedRoute` للصفحات المحمية
- تعريف جميع الـ routes

**الـ Routes:**
```
/login          → صفحة تسجيل الدخول (غير محمية)
/               → Dashboard (محمية)
/offers         → إدارة العروض (محمية)
/requests       → طلبات العملاء (محمية)
/matches        → التطابقات (محمية)
```

---

### 2️⃣ Context API - إدارة المصادقة

#### `AuthContext.jsx`
**المسؤوليات:**
- إدارة حالة المستخدم الحالي
- عمليات تسجيل الدخول والخروج
- حفظ واسترجاع التوكن من `localStorage`

**الوظائف المتاحة:**
```javascript
const { user, login, logout, loading } = useAuth();
```

| الخاصية | النوع | الوصف |
|-------|------|-------|
| `user` | Object | بيانات المستخدم (id, name, email, role) |
| `login(email, password)` | Function | تسجيل الدخول |
| `logout()` | Function | تسجيل الخروج وحذف التوكن |
| `loading` | Boolean | هل يتم التحقق من التوكن حالياً |

**مثال الاستخدام:**
```jsx
const { user, logout } = useAuth();

if (!user) {
  return <Navigate to="/login" />;
}
```

---

### 3️⃣ Utils - الدوال المساعدة

#### `api.js` - Axios Configuration
**ما يفعله:**
- إنشاء instance من axios بـ baseURL مشار إليه
- إضافة JWT Token تلقائياً إلى كل طلب

```javascript
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
```

**الاستخدام:**
```javascript
// Get
const { data } = await api.get('/offers');

// Post
await api.post('/offers', { type: 'Apartment', ... });

// Put
await api.put(`/offers/${id}`, { ...updates });

// Delete
await api.delete(`/offers/${id}`);

// Patch
await api.patch(`/matches/${id}`, { status: 'CLOSED' });
```

---

### 4️⃣ Components - المكونات القابلة لإعادة الاستخدام

#### `Layout.jsx` - تخطيط الصفحة
**المهام:**
- عرض القائمة الجانبية (Sidebar)
- عرض رأس الصفحة مع اسم الصفحة الحالية
- عرض محتوى الصفحة الحالية (`Outlet`)

```jsx
<div className="dashboard-layout">
  <Sidebar />
  <main>
    <header>صفحة: {getPageTitle()}</header>
    <Outlet />  {/* محتوى الصفحة الحالية */}
  </main>
</div>
```

---

#### `Sidebar.jsx` - القائمة الجانبية
**المهام:**
- عرض قائمة التنقل الرئيسية
- عرض معلومات المستخدم الحالي
- زر تسجيل الخروج

**الروابط المتاحة:**
- 📊 لوحة التحكم
- 🏢 العروض العقارية
- 🔍 طلبات العملاء
- 🤝 التطابقات الذكية
- 👥 المستخدمين (للـ ADMIN فقط)

**الأيقونات المستخدمة:**
- من مكتبة `phosphor-react`
- `Buildings`, `SquaresFour`, `MagnifyingGlass`, `Handshake`, `Users`, `SignOut`

---

#### `Modal.jsx` - نافذة منبثقة
**الخصائص:**
```jsx
<Modal isOpen={true} onClose={handleClose} title="عنوان النافذة">
  {/* المحتوى */}
</Modal>
```

| الخاصية | النوع | الوصف |
|-------|------|-------|
| `isOpen` | Boolean | هل النافذة مفتوحة؟ |
| `onClose` | Function | دالة الإغلاق |
| `title` | String | عنوان النافذة |
| `children` | JSX | محتوى النافذة |

---

#### `Table.jsx` - جدول ذكي
**الخصائص:**
```jsx
<Table 
  columns={[...]}  // تعريف الأعمدة
  data={[...]}     // البيانات
  actions={(row) => (...)}  // إجراءات إضافية (اختياري)
/>
```

**مثال على الأعمدة:**
```javascript
const columns = [
  { 
    header: 'الاسم', 
    accessor: 'name'  // اسم الخاصية في البيانات
  },
  { 
    header: 'السعر', 
    render: (row) => `${row.price.toLocaleString()} ج.م`  // تنسيق مخصص
  },
];
```

**مثال على الإجراءات:**
```javascript
const actions = (row) => (
  <>
    <button onClick={() => handleEdit(row.id)}>تعديل</button>
    <button onClick={() => handleDelete(row.id)}>حذف</button>
  </>
);
```

---

### 5️⃣ Pages - صفحات التطبيق

#### `Login.jsx` - صفحة تسجيل الدخول
**المهام:**
- عرض نموذج تسجيل الدخول
- التحقق من البيانات
- الاتصال بـ Backend للمصادقة
- حفظ التوكن وتحويل المستخدم

**الحقول:**
- البريد الإلكتروني
- كلمة المرور

**الأخطاء:**
- تعرض رسالة خطأ عند فشل تسجيل الدخول

---

#### `Offers.jsx` - إدارة العروض
**المهام:**
- عرض قائمة بجميع العروض العقارية
- إضافة عرض جديد
- تحديث وحذف العروض (مخطط)

**الحقول المتاحة:**
- عنوان العرض
- السعر (من - إلى)
- المساحة (من - إلى)
- المنطقة
- النوع (بيع/إيجار)
- نوع العقار (شقة، فيلا، إلخ)

**الميزات الحالية:**
✅ عرض جميع العروض في جدول
✅ إضافة عرض جديد عبر Modal
❌ تحديث عرض (مخطط)
❌ حذف عرض (مخطط)
❌ البحث والفلترة (مخطط)

---

#### `Requests.jsx` - طلبات العملاء
**المهام:**
- عرض قائمة بجميع طلبات العملاء
- إضافة طلب جديد
- تحديث وحذف الطلبات (مخطط)

**الحقول المتاحة:**
- اسم العميل
- نطاق السعر (من - إلى)
- المساحة المطلوبة (من - إلى)
- المنطقة المفضلة
- النوع (شراء/إيجار)
- نوع العقار المطلوب

**الميزات الحالية:**
✅ عرض جميع الطلبات في جدول
✅ إضافة طلب جديد عبر Modal
❌ تحديث طلب (مخطط)
❌ حذف طلب (مخطط)

---

#### `Matches.jsx` - التطابقات الذكية
**المهام:**
- عرض قائمة بجميع المطابقات بين العروض والطلبات
- تحديث حالة المطابقة

**المعلومات المعروضة:**
- العرض (الوحدة والسعر)
- الطلب (العميل والميزانية)
- نسبة التوافق (0-100%)
- الحالة الحالية

**حالات المطابقة:**
| الحالة | الوصف | اللون |
|------|-------|-------|
| NEW | جديد | أزرق |
| CONTACTED | تم التواصل | أصفر |
| NEGOTIATION | تفاوض | أحمر |
| CLOSED | تمت الصفقة | أخضر |
| REJECTED | مرفوض | رمادي |

**الميزات الحالية:**
✅ عرض جميع المطابقات
✅ تحديث حالة المطابقة عبر Dropdown
❌ تصفية حسب الحالة (مخطط)
❌ معلومات تفصيلية عن المطابقة (مخطط)

---

### 6️⃣ Styles - الأنماط

#### `Login.css`
- أنماط صفحة تسجيل الدخول
- أنماط النماذج (form styles)
- يُعاد استخدام هذا الملف في صفحات الإضافة

#### `Layout.css`
- أنماط التخطيط العام (Dashboard Layout)
- أنماط الـ Sidebar
- أنماط رأس الصفحة
- أنماط الأزرار الأساسية

#### `Modal.css`
- أنماط النوافذ المنبثقة
- الخلفية الشفافة (overlay)
- تمركز النافذة في الشاشة

#### `Table.css`
- أنماط الجداول
- تنسيق الصفوف والأعمدة
- حالة الخلايا الفارغة

---

## 🔧 كيفية الإضافة والتطوير

### إضافة صفحة جديدة

#### 1️⃣ إنشاء الملف
```jsx
// src/pages/NewPage.jsx
import { useState, useEffect } from 'react';
import api from '../utils/api';
import { useAuth } from '../context/AuthContext';

const NewPage = () => {
  const { user } = useAuth();
  const [data, setData] = useState([]);

  useEffect(() => {
    // جلب البيانات من Backend
    const fetchData = async () => {
      try {
        const { data } = await api.get('/endpoint');
        setData(data);
      } catch (error) {
        console.error('Error:', error);
      }
    };
    fetchData();
  }, []);

  return (
    <div>
      {/* المحتوى */}
    </div>
  );
};

export default NewPage;
```

#### 2️⃣ إضافة الـ Route
```jsx
// src/App.jsx
import NewPage from './pages/NewPage';

<Route element={<ProtectedRoute><Layout /></ProtectedRoute>}>
  <Route path="/new-page" element={<NewPage />} />
</Route>
```

#### 3️⃣ إضافة الرابط في Sidebar
```jsx
// src/components/Sidebar.jsx
<NavLink to="/new-page" className={...}>
  <Icon />
  <span>صفحة جديدة</span>
</NavLink>
```

#### 4️⃣ تحديث getPageTitle في Layout
```jsx
// src/components/Layout.jsx
const getPageTitle = () => {
  switch (location.pathname) {
    case '/new-page': return 'عنوان الصفحة';
    // ...
  }
};
```

---

### إضافة مكون جديد

#### مثال: مكون بطاقة (Card Component)
```jsx
// src/components/Card.jsx
const Card = ({ title, children, actions }) => {
  return (
    <div className="card">
      <div className="card-header">
        <h3>{title}</h3>
      </div>
      <div className="card-body">
        {children}
      </div>
      {actions && <div className="card-footer">{actions}</div>}
    </div>
  );
};

export default Card;
```

#### الاستخدام:
```jsx
<Card title="عنوان البطاقة">
  <p>محتوى البطاقة</p>
</Card>
```

---

## 🚀 تشغيل التطبيق

### التثبيت
```bash
cd frontend
npm install
```

### التطوير
```bash
npm run dev
```
- يفتح على `http://localhost:5173`

### البناء للإنتاج
```bash
npm run build
```

### التحقق من الأخطاء
```bash
npm run lint
```

---

## 📦 المكتبات المستخدمة

| المكتبة | الإصدار | الاستخدام |
|--------|--------|---------|
| React | ^19.2.0 | إطار العمل الأساسي |
| React Router | ^7.13.0 | التوجيه (Routing) |
| Axios | ^1.13.3 | طلبات HTTP |
| Phosphor React | ^1.4.1 | الأيقونات |
| Vite | ^7.2.4 | بناء المشروع |

---

## 🎨 نظام الألوان والأنماط

### متغيرات CSS (يجب إضافتها)
```css
:root {
  --primary-color: #3b82f6;      /* الأزرق الأساسي */
  --secondary-color: #8b5cf6;    /* البنفسجي */
  --success-color: #10b981;      /* الأخضر */
  --danger-color: #ef4444;       /* الأحمر */
  --warning-color: #f59e0b;      /* البرتقالي */
  --dark-bg: #1f2937;            /* الخلفية الداكنة */
  --light-bg: #f9fafb;           /* الخلفية الفاتحة */
  --border-color: #e5e7eb;       /* لون الحدود */
}
```

---

## ⚠️ ملاحظات مهمة

### 1️⃣ حقول النموذج (Form Fields)
الحقول الحالية في الـ Offers و Requests **لا تطابق** البيانات الفعلية من Backend:

**الـ Backend يتوقع:**
```json
{
  "type": "Apartment",
  "usage": "Residential",
  "landStatus": "Freehold",
  "city": "Cairo",
  "district": "Downtown",
  "areaFrom": 100,
  "areaTo": 150,
  "priceFrom": 1000000,
  "priceTo": 1500000
}
```

**الـ Frontend يرسل حالياً:**
```json
{
  "title": "...",
  "price": "...",
  "area": "...",
  "location": "...",
  "type": "...",
  "propertyType": "..."
}
```

**✅ يجب تحديث النماذج بسرعة!**

### 2️⃣ البحث والفلترة
لم يتم تطبيق البحث والفلترة في:
- صفحة العروض
- صفحة الطلبات
- صفحة المطابقات

**يمكن إضافة:**
- Filter Bar
- Search Input
- Filter Buttons

### 3️⃣ التحديث والحذف
عمليات التحديث والحذف لم تُطبق بعد في:
- صفحة العروض
- صفحة الطلبات

**يجب إضافة:**
- زر Edit يفتح Modal مع بيانات العنصر
- زر Delete مع تأكيد

### 4️⃣ صفحة لوحة التحكم
صفحة Dashboard موجودة في `App.jsx` كـ placeholder فقط
**يجب إنشاء صفحة منفصلة تعرض:**
- الإحصائيات (عدد العروض، الطلبات، المطابقات)
- أفضل السماسرة
- أفضل المناطق
- مخطط بياني للنشاط

### 5️⃣ إدارة المستخدمين
صفحة المستخدمين لم تُطبق بعد
**يجب إضافة:**
- عرض قائمة المستخدمين
- إضافة مستخدم جديد
- تعديل بيانات المستخدم
- حذف مستخدم

---

## 📋 قائمة المهام المتبقية

### ذات الأولوية العالية 🔴
- [ ] تحديث نماذج Offers بحقول Backend الصحيحة
- [ ] تحديث نماذج Requests بحقول Backend الصحيحة
- [ ] إضافة وظيفة التعديل (Edit) للعروض والطلبات
- [ ] إضافة وظيفة الحذف (Delete) مع التأكيد

### ذات الأولوية المتوسطة 🟡
- [ ] إنشاء صفحة Dashboard منفصلة بالإحصائيات
- [ ] إضافة البحث والفلترة للعروض والطلبات
- [ ] إنشاء صفحة إدارة المستخدمين
- [ ] إضافة صفحة للتنبيهات
- [ ] إضافة صفحة سجلات التدقيق

### ذات الأولوية المنخفضة 🟢
- [ ] تحسين التصميم والـ UI/UX
- [ ] إضافة رسائل نجاح وخطأ أفضل
- [ ] إضافة Loading states للعمليات
- [ ] إضافة Dark Mode
- [ ] تحسين Responsive Design

---

## 🔗 الربط مع Backend

### الـ Base URL
```javascript
// src/utils/api.js
const api = axios.create({
  baseURL: 'http://localhost:3000/api',
});
```

**تأكد من:**
- ✅ تشغيل Backend على `http://localhost:3000`
- ✅ وجود `/api` في جميع الـ routes

### الـ Token
يُحفظ تلقائياً في `localStorage` عند تسجيل الدخول
يُحذف تلقائياً عند تسجيل الخروج

---

## 💡 نصائح التطوير

### 1. استخدام React DevTools
- تحميل امتداد React DevTools للمتصفح
- تفتيش الـ components والـ state بسهولة

### 2. استخدام Axios Interceptors
يمكن إضافة معالج للأخطاء العامة:
```javascript
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      // انتهاء التوكن، إعادة توجيه للـ Login
    }
    return Promise.reject(error);
  }
);
```

### 3. التعامل مع الأخطاء
استخدم try-catch وأعرض رسائل خطأ واضحة للمستخدم

### 4. Loading States
أضف `loading` state عند جلب البيانات:
```javascript
const [loading, setLoading] = useState(false);

if (loading) return <div>جار التحميل...</div>;
```

---

## 📞 الدعم والمساعدة

للأسئلة أو المشاكل:
1. تحقق من Backend Endpoints Documentation
2. تأكد من توافق حقول النموذج مع Backend
3. افتح DevTools وتحقق من رسائل الخطأ
4. استخدم Network Tab للتحقق من الطلبات

---

**تم إنشاء هذه الوثيقة لمساعدتك على فهم البنية الحالية والاستمرار في التطوير**
